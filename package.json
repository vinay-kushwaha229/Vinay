{
  "name": "job-seeker-agency-platform",
  "version": "1.0.0",
  "description": "Job seeker and agency platform with chat functionality",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client",
    "server": "npm run dev --prefix server",
    "client": "npm start --prefix client",
    "dev": "echo Please run server and client in separate terminals"
  }
}
